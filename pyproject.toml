[project]
name = "synthetic-data-generator"
version = "0.1.0"
description = "Synthetic data generation and evaluation pipeline for Home DIY repair Q&A"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "instructor>=1.15.1",
    "logfire[openai]>=4.33.0",
    "openai>=1.0.0",
    "pydantic>=2.13.3",
    "python-dotenv>=1.0.0",
    "rich>=14.3.4",
]
